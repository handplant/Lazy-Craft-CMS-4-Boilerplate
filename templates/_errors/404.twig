{% extends "_layouts/base" %}

{% block main %}
  <div class="px-4 py-16 sm:px-6 sm:py-24 md:grid md:place-items-center lg:px-8">
    <div class="max-w-screen-lg mx-auto">
      <main class="sm:flex">
        <p class="mb-2 text-4xl font-extrabold text-alpha-600">
          404
        </p>
        <div class="sm:ml-6">
          <div class="sm:border-l sm:border-gray-200 sm:pl-6">
            {% if currentUser %}
              <div class="mb-3 text-2xl font-extrabold tracking-tight text-gray-900 sm:text-3xl">
                Sorry,
              </div>
            {% endif %}
            <h1 class="text-4xl font-extrabold tracking-tight text-gray-900 sm:text-5xl">
              This page could not be found.
            </h1>
            <p class="mt-1 text-base text-gray-500">
              Donec ullamcorper nulla non metus auctor fringilla.
            </p>
          </div>
          <div class="flex mt-10 space-x-3 sm:border-l sm:border-transparent sm:pl-6">
            {% set url = "/" %}
            {% set label = "Home" %}
            <a href="{{ url }}" class="btn">{{ label }}</a>
            {% if craft.app.request.getHeaders().referer %}
              <a href="{{ craft.app.request.getHeaders().referer }}" class="btn">Zurück</a>
            {% endif %}
          </div>
        </div>
      </main>
    </div>
  </div>
{% endblock %}
