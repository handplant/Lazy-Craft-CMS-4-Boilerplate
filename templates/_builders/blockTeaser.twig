<section class="blocks blocks--transparent">
  <div class="{{ style.blocks.lg }}">
    {% include "_components/teaser" with {
      data: {
        headline: self.headline,
        text: self.text,
        image: self.image.collect().first() ?? null,
        imageRight: self.imageRight,
        button: self.button
      }
    } only %}
  </div>
</section>
